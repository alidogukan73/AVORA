# AVORA sensor LAN listener. Install as /etc/mosquitto/conf.d/avora.conf only
# after both sensor firmwares and the Raspberry Pi have MQTT credentials.
per_listener_settings true
listener 1883
allow_anonymous false
password_file /etc/mosquitto/passwd
acl_file /etc/mosquitto/avora.acl

